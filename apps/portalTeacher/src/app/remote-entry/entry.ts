import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-portalTeacher-entry',
  template: `<router-outlet />`,
  imports: [RouterOutlet],
})
export class RemoteEntry {}
